name: Run Shell Commands
on: [push]

jobs :
  run-shell-command:
    runs-on: ubuntu:latest
    name: Run a shell command and print some text
    steps:
      - name: Echo a Staring
        run: echo "Hello-Github_Actions"
